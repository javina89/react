# react
React Boilerplate

```"devDependencies": {
    "@babel/cli": "^7.10.4", Transpile JS in the command line
    "@babel/core": "^7.10.4", Transpile JS programmatically and is needed for cli
    "@babel/preset-env": "^7.10.4", Allows ES6, ES7 and ES8
    "@babel/preset-react": "^7.10.4", Allows us to use JSX/XML syntax in components
    "babel-loader": "^8.1.0", For Webpack to transpile JS with Babel
    "css-loader": "^3.6.0", Returns a string
    "html-webpack-plugin": "^4.3.0", Generates an HTML file or specifies an existing one to serve the bundles
    "style-loader": "^1.2.1", Takes the string from css-loader and puts it in the style tag in the html
    "webpack": "^4.43.0", Webpack core functionality and is needed for cli
    "webpack-cli": "^3.3.12" Allows webpack commands via the command line
  },
  "dependencies": {
    "react": "^16.13.1", Library for making components
    "react-dom": "^16.13.1", Library that connects components to the DOM
    "react-router-dom": "^5.2.0" Allows routing in React
  }```